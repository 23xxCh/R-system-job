# R-system-job

R 语言求职市场数据分析项目，包含 Web 爬虫和 Shiny 仪表板。

## Agent skills

### Issue tracker

GitHub Issues — 使用 `gh` CLI 管理任务。See `docs/agents/issue-tracker.md`.

### Triage labels

默认标签体系：needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix. See `docs/agents/triage-labels.md`.

### Domain docs

单上下文 — 一个 CONTEXT.md + docs/adr/ 目录。See `docs/agents/domain.md`.
